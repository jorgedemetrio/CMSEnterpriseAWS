apiVersion: v1
kind: Service
metadata:
  name: backend-forum
  namespace: cmsaws
spec:
  selector:
    app: backend-forum
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
