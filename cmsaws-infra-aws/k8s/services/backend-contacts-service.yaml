apiVersion: v1
kind: Service
metadata:
  name: backend-contacts
  namespace: cmsaws
spec:
  selector:
    app: backend-contacts
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
