apiVersion: v1
kind: Service
metadata:
  name: frontend-admin
  namespace: cmsaws
spec:
  selector:
    app: frontend-admin
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
