apiVersion: v1
kind: Service
metadata:
  name: frontend-user
  namespace: cmsaws
spec:
  selector:
    app: frontend-user
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
