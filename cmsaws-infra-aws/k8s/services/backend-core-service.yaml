apiVersion: v1
kind: Service
metadata:
  name: backend-core
  namespace: cmsaws
spec:
  selector:
    app: backend-core
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
