apiVersion: v1
kind: Service
metadata:
  name: backend-users
  namespace: cmsaws
spec:
  selector:
    app: backend-users
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
